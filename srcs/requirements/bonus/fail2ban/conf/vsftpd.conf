[Definition]
# Example regex that matches typical vsftpd failed login attempts.
# Adjust the regex if your vsftpd log format differs.
failregex = ^.*vsftpd\[\d+\]:\s+FAIL LOGIN: Client "<HOST>" not allowed.*$
ignoreregex =
